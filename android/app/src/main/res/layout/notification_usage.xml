<?xml version="1.0" encoding="utf-8"?>
<!--
  COLLAPSED shade row — intentionally minimal so nothing clips:
    short title (e.g. "62% Used") + thin rounded-segment bar.
  Category dots / full reset text only in notification_usage_big.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:paddingEnd="4dp">

    <TextView
        android:id="@+id/notif_headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:textStyle="bold"
        android:maxLines="2"
        android:ellipsize="end" />

    <ImageView
        android:id="@+id/notif_bar"
        android:layout_width="match_parent"
        android:layout_height="10dp"
        android:scaleType="fitXY"
        android:contentDescription="@null" />
</LinearLayout>
